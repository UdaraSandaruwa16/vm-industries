interface contactInterface {
	data: string[];
}

interface contactProps {
	Navdata: contactInterface;
}

function ContactData({  }: contactProps) {
	return (
		<div >
			
		</div>
	);
}
export default ContactData;
